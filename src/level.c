/*
** EPITECH PROJECT, 2019
** level
** File description:
** level file
*/

#include "game.h"

void level1(global_t *data, bg_t *bg, mob_t *mobs)
{
    display_bck(data, bg, mobs);
    disp_score(data, bg);
}

void level2(global_t *data, bg_t *bg, mob_t *mobs)
{
    display_bck2(data, bg, mobs);
    disp_score(data, bg);
}

void level3(global_t *data, bg_t *bg, mob_t *mobs)
{
    display_bck3(data, bg, mobs);
    disp_score(data, bg);
}

void level4(global_t *data, bg_t *bg, mob_t *mobs)
{
    display_bck4(data, bg, mobs);
    disp_score(data, bg);
}

void level5(global_t *data, bg_t *bg, mob_t *mobs)
{
    display_bck5(data, bg, mobs);
    disp_score(data, bg);
}